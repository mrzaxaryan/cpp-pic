PHDRS {
    text PT_LOAD FLAGS(5);   /* R + X */
}

SECTIONS {
    .text : 
    {
        KEEP(*(.text$start)) /* Simple hack to keep main at the start of .text section */
        *(.text*)           /* Rest of the .text section */
        *(.rodata*)         /* Read-only data section */
        *(.bss*)         /* Read-only data section */
    } : text
}