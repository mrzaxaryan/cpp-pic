// Test string indexing
var s = "Hello";

// Access individual characters
var ok1 = s[0] == "H" && s[1] == "e" && s[4] == "o";

// Verify all characters via loop
var ok2 = true;
var expected = ["H", "e", "l", "l", "o"];
for (var i = 0; i < len(s); i = i + 1) {
    if (s[i] != expected[i]) {
        ok2 = false;
    }
}

var result = ok1 && ok2;
