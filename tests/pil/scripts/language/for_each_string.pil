// Test for-each over strings
var s = "Hello";

// Count characters
var count = 0;
for (var c in s) {
    count = count + 1;
}
var ok1 = count == 5;

// Count vowels in "Hello World" - e, o, o = 3 vowels
var vowels = 0;
var text = "Hello World";
for (var ch in text) {
    if (ch == "a" || ch == "e" || ch == "i" || ch == "o" || ch == "u" ||
        ch == "A" || ch == "E" || ch == "I" || ch == "O" || ch == "U") {
        vowels = vowels + 1;
    }
}
var ok2 = vowels == 3;

var result = ok1 && ok2;
