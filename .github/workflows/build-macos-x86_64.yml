name: macOS x86_64

on:
  pull_request:
    types: [opened, synchronize, reopened]
  push:
    branches:
      - "**"

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-and-test:
    runs-on: macos-15-intel
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install LLVM and build tools
        env:
          HOMEBREW_NO_AUTO_UPDATE: 1
        run: |
          for i in 1 2 3; do
            brew install llvm ninja cmake && break
            echo "Attempt $i failed, retrying in 10s..."
            sleep 10
          done
          echo "$(brew --prefix llvm)/bin" >> $GITHUB_PATH

      - name: Build -O0
        run: |
          rm -rf build
          cmake --preset macos-x86_64-release -DOPTIMIZATION_LEVEL=O0
          cmake --build --preset macos-x86_64-release

      - name: Upload Artifacts -O0
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: build-macos-x86_64-O0
          retention-days: 1
          path: |
            build/
            !build/**/cmake/

      - name: Test -O0 macho
        if: always()
        run: |
          TEST_BIN=$(find build -type f -name "output" ! -name "*.bin" ! -name "*.txt" | head -n 1)
          echo "Mach-O: $TEST_BIN ($(wc -c < "$TEST_BIN") bytes)"
          file "$TEST_BIN"
          llvm-objdump -h "$TEST_BIN" 2>/dev/null || true
          chmod +x "$TEST_BIN"
          codesign --sign - --force "$TEST_BIN"
          codesign --verify --verbose "$TEST_BIN" || echo "WARNING: codesign verification failed"
          "$TEST_BIN"

      - name: Test -O0 pic
        if: always()
        run: |
          PIC_BIN=$(find build -type f -name "output.bin" | head -n 1)
          echo "PIC: $PIC_BIN ($(wc -c < "$PIC_BIN") bytes)"
          python3 scripts/loader.py --arch x86_64 "$PIC_BIN"

      - name: Build -O1
        if: always()
        run: |
          rm -rf build
          cmake --preset macos-x86_64-release -DOPTIMIZATION_LEVEL=O1
          cmake --build --preset macos-x86_64-release

      - name: Upload Artifacts -O1
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: build-macos-x86_64-O1
          retention-days: 1
          path: |
            build/
            !build/**/cmake/

      - name: Test -O1 macho
        if: always()
        run: |
          TEST_BIN=$(find build -type f -name "output" ! -name "*.bin" ! -name "*.txt" | head -n 1)
          echo "Mach-O: $TEST_BIN ($(wc -c < "$TEST_BIN") bytes)"
          file "$TEST_BIN"
          llvm-objdump -h "$TEST_BIN" 2>/dev/null || true
          chmod +x "$TEST_BIN"
          codesign --sign - --force "$TEST_BIN"
          codesign --verify --verbose "$TEST_BIN" || echo "WARNING: codesign verification failed"
          "$TEST_BIN"

      - name: Test -O1 pic
        if: always()
        run: |
          PIC_BIN=$(find build -type f -name "output.bin" | head -n 1)
          echo "PIC: $PIC_BIN ($(wc -c < "$PIC_BIN") bytes)"
          python3 scripts/loader.py --arch x86_64 "$PIC_BIN"

      - name: Build -O2
        if: always()
        run: |
          rm -rf build
          cmake --preset macos-x86_64-release -DOPTIMIZATION_LEVEL=O2
          cmake --build --preset macos-x86_64-release

      - name: Upload Artifacts -O2
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: build-macos-x86_64-O2
          retention-days: 1
          path: |
            build/
            !build/**/cmake/

      - name: Test -O2 macho
        if: always()
        run: |
          TEST_BIN=$(find build -type f -name "output" ! -name "*.bin" ! -name "*.txt" | head -n 1)
          echo "Mach-O: $TEST_BIN ($(wc -c < "$TEST_BIN") bytes)"
          file "$TEST_BIN"
          llvm-objdump -h "$TEST_BIN" 2>/dev/null || true
          chmod +x "$TEST_BIN"
          codesign --sign - --force "$TEST_BIN"
          codesign --verify --verbose "$TEST_BIN" || echo "WARNING: codesign verification failed"
          "$TEST_BIN"

      - name: Test -O2 pic
        if: always()
        run: |
          PIC_BIN=$(find build -type f -name "output.bin" | head -n 1)
          echo "PIC: $PIC_BIN ($(wc -c < "$PIC_BIN") bytes)"
          python3 scripts/loader.py --arch x86_64 "$PIC_BIN"

      - name: Build -O3
        if: always()
        run: |
          rm -rf build
          cmake --preset macos-x86_64-release -DOPTIMIZATION_LEVEL=O3
          cmake --build --preset macos-x86_64-release

      - name: Upload Artifacts -O3
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: build-macos-x86_64-O3
          retention-days: 1
          path: |
            build/
            !build/**/cmake/

      - name: Test -O3 macho
        if: always()
        run: |
          TEST_BIN=$(find build -type f -name "output" ! -name "*.bin" ! -name "*.txt" | head -n 1)
          echo "Mach-O: $TEST_BIN ($(wc -c < "$TEST_BIN") bytes)"
          file "$TEST_BIN"
          llvm-objdump -h "$TEST_BIN" 2>/dev/null || true
          chmod +x "$TEST_BIN"
          codesign --sign - --force "$TEST_BIN"
          codesign --verify --verbose "$TEST_BIN" || echo "WARNING: codesign verification failed"
          "$TEST_BIN"

      - name: Test -O3 pic
        if: always()
        run: |
          PIC_BIN=$(find build -type f -name "output.bin" | head -n 1)
          echo "PIC: $PIC_BIN ($(wc -c < "$PIC_BIN") bytes)"
          python3 scripts/loader.py --arch x86_64 "$PIC_BIN"

      - name: Build -Os
        if: always()
        run: |
          rm -rf build
          cmake --preset macos-x86_64-release -DOPTIMIZATION_LEVEL=Os
          cmake --build --preset macos-x86_64-release

      - name: Upload Artifacts -Os
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: build-macos-x86_64-Os
          retention-days: 1
          path: |
            build/
            !build/**/cmake/

      - name: Test -Os macho
        if: always()
        run: |
          TEST_BIN=$(find build -type f -name "output" ! -name "*.bin" ! -name "*.txt" | head -n 1)
          echo "Mach-O: $TEST_BIN ($(wc -c < "$TEST_BIN") bytes)"
          file "$TEST_BIN"
          llvm-objdump -h "$TEST_BIN" 2>/dev/null || true
          chmod +x "$TEST_BIN"
          codesign --sign - --force "$TEST_BIN"
          codesign --verify --verbose "$TEST_BIN" || echo "WARNING: codesign verification failed"
          "$TEST_BIN"

      - name: Test -Os pic
        if: always()
        run: |
          PIC_BIN=$(find build -type f -name "output.bin" | head -n 1)
          echo "PIC: $PIC_BIN ($(wc -c < "$PIC_BIN") bytes)"
          python3 scripts/loader.py --arch x86_64 "$PIC_BIN"

      - name: Build -Oz
        if: always()
        run: |
          rm -rf build
          cmake --preset macos-x86_64-release -DOPTIMIZATION_LEVEL=Oz
          cmake --build --preset macos-x86_64-release

      - name: Upload Artifacts -Oz
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: build-macos-x86_64-Oz
          retention-days: 1
          path: |
            build/
            !build/**/cmake/

      - name: Test -Oz macho
        if: always()
        run: |
          TEST_BIN=$(find build -type f -name "output" ! -name "*.bin" ! -name "*.txt" | head -n 1)
          echo "Mach-O: $TEST_BIN ($(wc -c < "$TEST_BIN") bytes)"
          file "$TEST_BIN"
          llvm-objdump -h "$TEST_BIN" 2>/dev/null || true
          chmod +x "$TEST_BIN"
          codesign --sign - --force "$TEST_BIN"
          codesign --verify --verbose "$TEST_BIN" || echo "WARNING: codesign verification failed"
          "$TEST_BIN"

      - name: Test -Oz pic
        if: always()
        run: |
          PIC_BIN=$(find build -type f -name "output.bin" | head -n 1)
          echo "PIC: $PIC_BIN ($(wc -c < "$PIC_BIN") bytes)"
          python3 scripts/loader.py --arch x86_64 "$PIC_BIN"

      - name: Build -Og
        if: always()
        run: |
          rm -rf build
          cmake --preset macos-x86_64-release -DOPTIMIZATION_LEVEL=Og
          cmake --build --preset macos-x86_64-release

      - name: Upload Artifacts -Og
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: build-macos-x86_64-Og
          retention-days: 1
          path: |
            build/
            !build/**/cmake/

      - name: Test -Og macho
        if: always()
        run: |
          TEST_BIN=$(find build -type f -name "output" ! -name "*.bin" ! -name "*.txt" | head -n 1)
          echo "Mach-O: $TEST_BIN ($(wc -c < "$TEST_BIN") bytes)"
          file "$TEST_BIN"
          llvm-objdump -h "$TEST_BIN" 2>/dev/null || true
          chmod +x "$TEST_BIN"
          codesign --sign - --force "$TEST_BIN"
          codesign --verify --verbose "$TEST_BIN" || echo "WARNING: codesign verification failed"
          "$TEST_BIN"

      - name: Test -Og pic
        if: always()
        run: |
          PIC_BIN=$(find build -type f -name "output.bin" | head -n 1)
          echo "PIC: $PIC_BIN ($(wc -c < "$PIC_BIN") bytes)"
          python3 scripts/loader.py --arch x86_64 "$PIC_BIN"
