// Test mixed int/float arithmetic
var add = 42 + 3.14;
var mul = 10 * 0.5;
var sub = 7.5 - 2;
var div = 100 / 2.5;

var result = (add > 45.1) && (add < 45.2) &&
             (mul == 5) &&
             (sub == 5.5) &&
             (div == 40);
