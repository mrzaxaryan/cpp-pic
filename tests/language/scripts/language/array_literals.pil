// Test array literals
var empty = [];
var nums = [1, 2, 3, 4, 5];
var mixed = [1, "hello", true, nil];

var result = len(empty) == 0 &&
             len(nums) == 5 &&
             len(mixed) == 4 &&
             type(nums) == "array";
